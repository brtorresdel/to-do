type statusCss = {
    bgColor: string;
    text: string
};

type taskListCss = {
    bg: string;
    text: string;
    border: string;
    check: string
};

type actionBtnCss = {
    bg: string;
    color: string
};

type taskInfoCss = {
    bg: string;
    text: string
}

type taskFormCss = {
    bg: string;
    text: string
    border: string;
}

type inputCss = {
    bg: string;
    text: string;
    border: string;
}

type btnCss = {
    bg: string;
    textColor: string;
    svgColor: string;
}

export type { statusCss, taskListCss, actionBtnCss, taskInfoCss, taskFormCss, inputCss, btnCss };