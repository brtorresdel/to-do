type statusCss = {
    bgColor: string;
    text: string
};

type taskListCss = {
    bg: string;
    text: string;
    border: string;
    check: string
};

type actionBtnCss = {
    bg: string;
    color: string
};

type taskInfoCss = {
    bg: string;
    text: string
}

type taskFormCss = {
    bg: string;
    text: string
    border: string;
}

export type { statusCss, taskListCss, actionBtnCss, taskInfoCss, taskFormCss};